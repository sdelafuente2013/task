<div class="container mx-auto px-6 pt-6">
  <h2 class="text-4xl mb-6">Nueva receta</h2>

  <%= simple_form_for(@recipe, html: { class: "form" }) do |f| %>
    <div class="mb-4">
      <%= f.input :name, label: "Nombre", class: "shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline" %>
    </div>

    <div class="mb-6">
      <%= f.input :description, label: "Descripción", as: :text, input_html: { rows: 4 }, class: "shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline" %>
    </div>

    <div>
      <%= f.button :submit, "Crear Receta", class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>
    </div>
  <% end %>
</div>
